export const metadata = {
    title: "Credentials",
}

function Credentials() {
    return (
        <section>
            <h1>Credentials</h1>
        </section>
    );
}

export default Credentials;